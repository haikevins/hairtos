# Phase 13.3 — Flat State Machine

A two-state LED state machine demonstrates run-to-completion dispatch and the
ordered transition sequence `EXIT -> state change -> ENTRY`.

```bash
make EXAMPLE=13-03-flat-state-machine run
```
